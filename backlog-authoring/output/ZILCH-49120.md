# ZILCH-49120 — Fix fee-service synthetic test tagging

**Type:** Task
**Parent:** ZILCH-47400

---

## Context

Many synthetic tests were recently added to DataDog for the fee-service.

However, two issues exist with them:

- Tests are not attributed to the Merchant team.
- Tests are tagged with `rewards` rather than `fees`.

## Scope

Fix both issues.

## Implementation Notes

The fee calculation synthetic suite is defined in Terraform (`zilchdev/terraform-datadog`); changes should be made there rather than directly in the DataDog UI where possible.

---

## Acceptance Criteria

*(in Jira AC field)*
